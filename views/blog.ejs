<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('templates/head') %> 
  <link rel="stylesheet" href="./style/home.css">
</head>
<body>
  <%- include('templates/header') %> 
    <main>
        <h1>Welcome visitor, here are our blog posts. </h1>
        <% if (!posts) { %>
         <h1>No posts written yet, be the first!</h1>
        <% } else { %>
          <ol>
            <% for(let post of posts) { %>
              <li>
                <h3><%= post.title %></h3>
                <p><%= post.summary %></p>
                <i>Written by: <%= post.author.name %></i> || <i><%= post.date %></i>
                <form action="/post/<%= post._id %>/delete" method="post">
                  <input type="hidden" name="" id="">
                  <button>Delete</button>
                </form>
              </li>
              <% } %>
          </ol>
        <% } %>
        
    </main>
</body>
</html>